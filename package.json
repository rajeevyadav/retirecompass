{
  "name": "retirecompass",
  "private": true,
  "version": "1.1.0",
  "description": "No runtime dependencies — the app itself is plain HTML/CSS/JS with zero packages. jsdom below is a dev-only dependency for the tests/scenario_engine.js regression suite (tests/test_scenarios.py runs it via pytest); it is never shipped or loaded by index.html.",
  "devDependencies": {
    "jsdom": "^24.1.3"
  },
  "scripts": {
    "test:scenarios": "node tests/scenario_engine.js"
  }
}
