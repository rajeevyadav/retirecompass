# Build output
/dist/
/build/
*.spec
__pycache__/
*.pyc

# Dev-only test dependency (jsdom for tests/scenario_engine.js); installed
# with `npm install --no-save jsdom`, never shipped and never part of a release.
/node_modules/
package-lock.json

# Local env / editor
.env
.venv/
.vscode/
.idea/
.DS_Store
Thumbs.db

# Never commit private data or secrets
/private/
*.key
*.pem
secrets.*

# Local engineering-standards guidance — not published
CLAUDE.md
